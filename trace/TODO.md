# TODO List

* [x] add a subcommand `convert` that takes `--from nsys --to json` (default) that takes an nsys rep file and converts to chrome trace json. Add `-o/--output <json_file>` that writes the output there (overwrites if exists)
* [ ] verify conversion is done via `./trace.sh convert ~/trace.nsys-rep`
